/**
 * Enum for meal to specify their orders as well by value.
 * @readonly
 * @enum {number}
 */
export const MealType = Object.freeze({
  BREAKFAST: enumValue(1),
  LUNCH: enumValue(2),
  DINNER: enumValue(3),
});
